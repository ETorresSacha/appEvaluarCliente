import React from "react";
import { View, StyleSheet, ScrollView, Text } from "react-native";

const Calculator = () => {
  return (
    <View style={styles.container}>
      <Text>caluladora</Text>
    </View>
  );
};

export default Calculator;
const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
